name: radartui
description: A flutter-like TUI framework for dart.
version: 0.0.1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  collection: ^1.17.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
